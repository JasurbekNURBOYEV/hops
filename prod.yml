version: '3'

services:
  core:
    build: .
    ports:
      - '1243:1243'
    command: daphne -b 0.0.0.0 -p 1243 service.service.asgi:application
    environment:
      - PROD=1
    env_file:
      - .env